# Python
.venv/
.venv-yam/
__pycache__/
*.pyc

# Recorded datasets + generated web assets (regenerated by export_web)
yam/data/
yam/runs/
yam/sessions/
yam/web/public/model/

# Node (only if you ever add a build step to the viewer)
node_modules/
dist/

# Contains CURSOR_API_KEY for the chat agent -- never commit
.cursor/mcp.json

# Runtime-composed scenes (spawn_object)
yam/assets/yam/_live_*.xml

# MuJoCo writes this next to whatever invoked it
MUJOCO_LOG.TXT

# OS
.DS_Store
